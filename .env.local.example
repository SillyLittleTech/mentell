# Copy to .env.local (gitignored) for local weekly AI testing
VITE_ENABLE_WEEKLY_AI_SUMMARY=1
VITE_WEEKLY_AI_ENDPOINT=http://127.0.0.1:8787/weekly-summary
# Must match worker/.dev.vars WEEKLY_SUMMARY_TOKEN (quote if token contains #)
VITE_WEEKLY_AI_TOKEN=dev-local-token
